/***
 * Package used for Data Transfer Objects (DTO), used to carry data between processes without having
 * to send the actual objects. These DTOs correspond to the objects in the model package.
 */
package dto;
